name: PR Review Assignment (Manual)

# This workflow assigns reviewers and adds labels to pull requests.
#
# MANUAL TRIGGER ONLY - No cost, but kept manual for consistency
#
# Review Assignment Rules (updated to reduce reviewers):
# - Developer PRs: Architect and Tester (2 reviewers)
# - Architect PRs: Developer and Tester (2 reviewers)
# - Tester PRs: Developer and Product Owner (2 reviewers)
# - IT PRs: Architect (1 reviewer)
# - Product Owner PRs: Architect (1 reviewer)
#
# The workflow adds labels and creates a comment with review checklist.

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to add labels and reviewers'
        required: true
        type: number

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  assign-reviewers:
    name: Assign Peer Reviewers
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect Agent and Assign Reviewers
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const prNumber = ${{ inputs.pr_number }};

            // Get PR details
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            const branchName = pr.data.head.ref;
            const prAuthor = pr.data.user.login;
            const prTitle = pr.data.title;

            console.log(`Processing PR #${prNumber}: ${prTitle}`);
            console.log(`Branch: ${branchName}`);
            console.log(`Author: ${prAuthor}`);

            // Extract agent type from branch name pattern: {llm-name}/{agent-type}-{project}-{sessionID}
            // Examples: copilot/developer-sudoku-webapp-abc123, claude/architect-app-pbCFa
            const branchPattern = /^([a-z-]+)\/(developer|architect|tester|it|product-owner)-(.+)-(.+)$/;
            const match = branchName.match(branchPattern);

            if (!match) {
              console.log(`Branch "${branchName}" does not match expected pattern. Expected: {llm-name}/{agent-type}-{project}-{sessionID}`);
              console.log(`Valid agent types: developer, architect, tester, it, product-owner`);
              return;
            }

            const llmAgentName = match[1]; // e.g., "copilot", "claude", "gemini"
            const agentType = match[2]; // e.g., "developer", "architect", "tester", "it", "product-owner"
            const projectName = match[3]; // e.g., "rtdcs", "sudoku-webapp"
            const sessionId = match[4]; // e.g., "pbCFa", "1769889815", "abc-123"

            console.log(`LLM Agent: ${llmAgentName}, Agent Type: ${agentType}, Project: ${projectName}, Session: ${sessionId}`);

            // Define review assignment rules (reduced reviewers to save API costs)
            const reviewRules = {
              'developer': {
                reviewers: ['Architect', 'Tester'],
                labels: ['peer-review:developer', 'awaiting-architect-review', 'awaiting-tester-review'],
                description: 'Developer implementation PRs must be reviewed by Architect and Tester before merge.'
              },
              'architect': {
                reviewers: ['Developer', 'Tester'],
                labels: ['peer-review:architect', 'awaiting-developer-review', 'awaiting-tester-review'],
                description: 'Architect design PRs must be reviewed by Developer and Tester before merge.'
              },
              'tester': {
                reviewers: ['Developer', 'Product Owner'],
                labels: ['peer-review:tester', 'awaiting-developer-review', 'awaiting-product-owner-review'],
                description: 'Tester QA PRs must be reviewed by Developer and Product Owner before merge.'
              },
              'it': {
                reviewers: ['Architect'],
                labels: ['peer-review:it', 'awaiting-architect-review'],
                description: 'IT infrastructure PRs must be reviewed by Architect before merge.'
              },
              'product-owner': {
                reviewers: ['Architect'],
                labels: ['peer-review:product-owner', 'awaiting-architect-review'],
                description: 'Product Owner PRs must be reviewed by Architect before merge.'
              }
            };

            const reviewConfig = reviewRules[agentType];

            if (!reviewConfig) {
              console.log(`No review rules defined for agent type: ${agentType}`);
              return;
            }

            console.log(`Applying review rules for ${agentType}`);
            console.log(`Required reviewers: ${reviewConfig.reviewers.join(', ')}`);

            // Add labels to the PR
            try {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                labels: reviewConfig.labels
              });
              console.log(`Added labels: ${reviewConfig.labels.join(', ')}`);
            } catch (error) {
              console.error('Failed to add labels:', error.message);
            }

            // Create review request comment
            const reviewersMarkdown = reviewConfig.reviewers.map(r => `- **${r} Agent**`).join('\n');

            const commentBody = `## 🔍 Peer Review Required

            This PR is from the **${agentType.charAt(0).toUpperCase() + agentType.slice(1)} Agent** and requires peer review before merging.

            ### Required Reviewers

            ${reviewersMarkdown}

            ### Review Policy

            ${reviewConfig.description}

            ### Review Process

            1. **Each reviewer** should thoroughly review the code, design, tests, and documentation
            2. **Approve or request changes** using GitHub's review feature
            3. **Address feedback** by the ${agentType.charAt(0).toUpperCase() + agentType.slice(1)} Agent
            4. **Obtain all approvals** (minimum ${reviewConfig.reviewers.length} approvals required)
            5. **Merge** only after all reviews are approved

            ### Review Checklist

            **Product Owner** (if assigned):
            - [ ] Code follows project standards and conventions
            - [ ] Design patterns and SOLID principles are correctly applied
            - [ ] Documentation is clear and comprehensive
            - [ ] Overall quality meets project requirements

            **Architect** (if assigned):
            - [ ] Design adheres to EDS specifications
            - [ ] Interfaces are correctly implemented
            - [ ] Design patterns are appropriate and well-applied
            - [ ] SOLID principles are followed
            - [ ] Architecture is sound and maintainable

            **Developer** (if assigned):
            - [ ] Code is clean, readable, and maintainable
            - [ ] Implementation follows specifications
            - [ ] Error handling is comprehensive
            - [ ] No code smells or anti-patterns
            - [ ] Unit tests are present and comprehensive

            **Tester** (if assigned):
            - [ ] Code is testable
            - [ ] Test coverage is adequate (>80%)
            - [ ] Edge cases are handled
            - [ ] Integration points are properly tested
            - [ ] Quality gates are met

            ---

            **Branch**: \`${branchName}\`
            **Project**: \`${projectName}\`
            **Session ID**: \`${sessionId}\`

            *This is an automated peer review assignment. Please complete your reviews before this PR can be merged.*
            `;

            try {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: commentBody
              });
              console.log('Created peer review request comment');
            } catch (error) {
              console.error('Failed to create comment:', error.message);
            }

            // Set PR to draft if not already (optional - enforces review before merge)
            // Disabled by default - uncomment if you want to enforce draft state
            /*
            if (!context.payload.pull_request.draft) {
              try {
                await github.rest.pulls.update({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  pull_number: prNumber,
                  draft: true
                });
                console.log('Set PR to draft state (requires reviews before ready for merge)');
              } catch (error) {
                console.error('Failed to set draft state:', error.message);
              }
            }
            */

            console.log('✅ Peer review assignment completed successfully');

      - name: Summary
        run: |
          echo "## ✅ Peer Review Assignment Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Automatic reviewers have been assigned to this PR based on the agent type." >> $GITHUB_STEP_SUMMARY
          echo "Please check the PR comments for the complete review checklist." >> $GITHUB_STEP_SUMMARY
