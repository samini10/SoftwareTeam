# Environment Variables Template
# Copy to .env and fill in your values
# IMPORTANT: .env is gitignored - never commit API keys!

# =============================================================================
# LLM Provider Configuration (MANDATORY)
# =============================================================================

# Choose your LLM provider: openai, anthropic, gemini, azure, cohere, mistral
LLM_PROVIDER=openai

# Generic LLM API Key - works with all providers
# This is the ONLY API key variable you need to set
LLM_API_KEY=

# Azure-specific (only if LLM_PROVIDER=azure)
AZURE_OPENAI_ENDPOINT=

# =============================================================================
# GitHub (for automated PR creation)
# =============================================================================

# GitHub Personal Access Token
# Create at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=

# =============================================================================
# Usage
# =============================================================================
#
# Option 1: Source the .env file
#   export $(cat .env | xargs)
#
# Option 2: Add to shell profile (~/.bashrc, ~/.zshrc)
#   export LLM_PROVIDER="openai"
#   export LLM_API_KEY="your-key"
#
# Option 3: Use a tool like direnv
#   echo "dotenv" > .envrc
#   direnv allow

